# Week 8 — Sprint 3: Advanced Queries & Performance

## Overview

Week 8 begins Phase 3 Sprint 3 for the **Cardiac Patient Monitoring System API**.

The week started with Sprint 3 planning and a focus on database query performance using Entity Framework Core.

Day 1 focused on defining measurable performance targets, enabling EF Core query logging, preparing realistic test data, measuring the actual SQL query count of important list endpoints, and diagnosing the N+1 query problem.

The existing list endpoints were reviewed and measured first. No N+1 problem was found in the tested production endpoints, so a dedicated diagnostic VitalSigns endpoint was introduced specifically to reproduce and measure the N+1 pattern without misrepresenting the existing implementation.

The diagnostic endpoint produced 51 SQL queries for 50 VitalSign records, establishing a clear baseline for the optimization work that will continue during Sprint 3.

Day 2 focused on fixing the diagnosed N+1 query problem using eager loading with `Include`, comparing eager loading with projection using `Select`, and measuring the actual before-and-after SQL query counts.

The diagnostic VitalSigns endpoint was reduced from 51 SQL queries to 1 query using `Include`. The same endpoint was then converted to projection, which also used 1 SQL query while selecting only the required fields. `AsSplitQuery` was reviewed but not applied because the current project does not contain an endpoint that includes two or more collection navigation properties in the same query.

## Daily Work

| Day   | Topic                                             | Project / Documentation |
| ----- | ------------------------------------------------- | ----------------------- |
| Day 1 | Sprint 3 Planning & Diagnosing the N+1 Problem    | [View Day 1](./Day%201) |
| Day 2 | Query Optimization with Eager & Explicit Loading | [View Day 2](./Day%202) |

## Week 8 Highlights

### Sprint 3 Planning

- Defined the Sprint 3 performance-focused goal.
- Created the Sprint 3 backlog in Notion.
- Carried forward the Sprint 2 retrospective improvement action.
- Defined measurable database query performance targets.
- Planned to measure SQL query counts before and after optimization.
- Planned to test query behavior using realistic data volumes of at least 50 records.

### Sprint 2 Retrospective Action

The following concrete improvement action was carried forward from Sprint 2:

```text
Write an explicit ownership-check test for every new patient-specific resource endpoint.
```

### EF Core Query Logging

- Enabled Entity Framework Core SQL query logging.
- Used `LogTo` with `LogLevel.Information`.
- Enabled sensitive data logging for development diagnostics.
- Used the application console to inspect the actual SQL generated by EF Core.
- Used the generated SQL logs to count database queries instead of estimating query behavior from application code alone.

### Realistic Performance Test Data

- Extended the existing `DbSeeder` for performance testing.
- Seeded at least 50 VitalSign records.
- Seeded at least 50 Medication records.
- Seeded at least 50 Appointment records.
- Used count-based seeding logic to insert only the missing records.
- Prepared enough records to make repeated-query behavior visible during performance diagnosis.

### List Endpoint Query Measurements

Three important list endpoints were tested while monitoring the EF Core SQL logs.

| Endpoint | SQL Queries | N+1 Detected |
| -------- | ----------: | ------------ |
| `GET /api/Appointments` | 2 | No |
| `GET /api/VitalSigns` | 1 | No |
| `GET /api/Medications` | 1 | No |

### Appointment Query Measurement

- Tested `GET /api/Appointments`.
- Confirmed that the endpoint generated two SQL queries.
- One query retrieves the total appointment count.
- One query retrieves the requested paginated result.
- Confirmed that the query count remains fixed rather than increasing with the number of returned records.
- Confirmed that the endpoint does not contain an N+1 query pattern.

### VitalSigns Query Measurement

- Tested `GET /api/VitalSigns`.
- Confirmed that the endpoint generated one SQL query.
- Reviewed `VitalSignService`.
- Reviewed `VitalSignRepository`.
- Confirmed that all VitalSign records are retrieved with a single `ToListAsync()` query.
- Confirmed that response mapping occurs in memory and does not generate additional database queries.
- Confirmed that the endpoint does not contain an N+1 query pattern.

### Medications Query Measurement

- Tested `GET /api/Medications`.
- Confirmed that the endpoint generated one SQL query.
- Reviewed the existing EF Core projection used by `MedicationService`.
- Confirmed that the required response fields are selected directly in the database query.
- Confirmed that the endpoint does not contain an N+1 query pattern.

### Existing Endpoint Review

The existing list implementation was reviewed for possible N+1 query patterns.

The following components were checked:

- `PatientService`
- `AppointmentService`
- `VitalSignService`
- `VitalSignRepository`
- `MedicationService`

The review confirmed that the tested existing list endpoints were already avoiding the N+1 query problem.

The project also does not use EF Core lazy-loading proxies, so accessing navigation properties does not automatically trigger additional database queries.

### N+1 Diagnostic Scenario

Because no N+1 issue was found in the tested existing list endpoints, a dedicated diagnostic endpoint was introduced specifically for Sprint 3 performance analysis:

```http
GET /api/VitalSigns/diagnostic-n-plus-one
```

The diagnostic implementation first retrieves all VitalSign records using one database query.

It then loops through the returned VitalSigns and performs a separate Patient query for every record.

This intentionally reproduces the N+1 query pattern for measurement and optimization training.

With 50 VitalSign records:

```text
1 query  → Load all VitalSigns
50 queries → Query Patient data once for every VitalSign
```

The confirmed result was:

```text
Records: 50 VitalSigns
Actual SQL Queries: 51
N+1 Detected: Yes
Response Time: 234 ms
```

The repeated Patient SQL query was verified directly in the EF Core console logs.

This diagnostic endpoint was introduced specifically for the performance lab and does not represent a pre-existing N+1 defect in the original production endpoints.

### N+1 Performance Baseline

The Sprint 3 N+1 baseline was established as:

```text
Endpoint:
GET /api/VitalSigns/diagnostic-n-plus-one

Records:
50 VitalSigns

SQL Queries:
51

N+1:
Confirmed

Baseline Response Time:
234 ms
```

The SQL query count is the primary measurement used for the optimization comparison.

The response time is recorded as an observed baseline but may vary between executions.

### N+1 Optimization with Include

The N+1 diagnostic endpoint from Day 1 was optimized using eager loading.

The related Patient entity was loaded together with each VitalSign using:

```csharp
.Include(v => v.Patient)
```

The generated SQL used an `INNER JOIN` between `VitalSigns` and `Patients`.

The measured result was:

```text
Records: 50 VitalSigns
SQL Queries: 1
N+1 Detected: No
Observed Response Time: 228 ms
```

The SQL query count dropped from:

```text
51 → 1
```

### Projection Optimization

The same list-style endpoint was then converted from eager loading to projection using `Select`.

The projection returned only the fields required by the response:

- VitalSign ID
- Patient ID
- Heart rate
- Recorded date
- Patient name

The generated SQL selected only those required columns while still using one database query.

The measured result was:

```text
Records: 50 VitalSigns
SQL Queries: 1
N+1 Detected: No
Observed Response Time: 35 ms
```

Projection was selected as the leaner approach for this list-style endpoint because it avoids loading unnecessary entity columns.

### Include vs Projection

Both approaches successfully removed the N+1 problem.

| Approach | SQL Queries | Data Retrieved |
| -------- | ----------: | -------------- |
| Original N+1 implementation | 51 | VitalSigns plus repeated Patient queries |
| `Include` | 1 | Full VitalSign and Patient entity data |
| Projection with `Select` | 1 | Only required response fields |

The SQL query count was used as the primary performance metric.

Observed response times were recorded for reference but were not treated as the primary comparison because execution time can vary between runs.

### Split Query Review

`AsSplitQuery` was reviewed as part of Day 2.

It is useful when a query includes two or more collection navigation properties and a single JOIN could create a cartesian-product explosion.

`AsSplitQuery` was not applied because the current project does not contain an endpoint that includes two or more collection navigation properties in the same query.

### Day 2 Performance Comparison

The complete before-and-after measurements were:

| Version | Records | SQL Queries | N+1 | Observed Response Time |
| ------- | ------: | ----------: | --- | ---------------------: |
| Before Optimization | 50 | 51 | Yes | 273 ms |
| After `Include` | 50 | 1 | No | 228 ms |
| After Projection | 50 | 1 | No | 35 ms |

The confirmed query-count improvement was:

```text
Before: 51 SQL Queries
After Include: 1 SQL Query
After Projection: 1 SQL Query
```

The SQL query count was treated as the primary optimization metric.

The observed response times were recorded for comparison, but they may vary between executions.

### Sprint 3 Backlog

The current Sprint 3 backlog includes:

| Backlog Item | Status |
| ------------ | ------ |
| Enable EF Core query logging in development | Done |
| Seed database with 50+ realistic test records | Done |
| Measure query counts for 2–3 important list endpoints | Done |
| Identify at least one N+1 query pattern | Done |
| Document the confirmed N+1 issue as a backlog task | Done |
| Optimize VitalSigns diagnostic N+1 query from 51 queries to a small fixed number | Done |
| Compare query counts before and after optimization | Done |
| Carry forward Sprint 2 ownership-check testing improvement action | To Do |

The initial query measurement and N+1 diagnosis work was completed during Day 1.

The N+1 optimization and before/after performance comparison were completed during Day 2.

The Sprint 2 ownership-check testing improvement action remains active for future patient-specific resource endpoints.

## Tools Used

- C#
- ASP.NET Core Web API
- Entity Framework Core
- SQL Server
- EF Core Query Logging
- `LogTo`
- `LogLevel.Information`
- `EnableSensitiveDataLogging`
- LINQ
- `AsNoTracking`
- `Include`
- `ThenInclude`
- `Select`
- Projection
- `AsSplitQuery`
- Visual Studio
- Swagger
- Notion
- Git
- GitHub
